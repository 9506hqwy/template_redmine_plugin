{
    "name": "template_redmine_plugin",
    "scripts": {
        "check": "biome check --write",
        "format": "biome format --write",
        "lint": "biome lint --write"
    },
    "devDependencies": {
        "@biomejs/biome": "2.3.8",
        "@playwright/test": "^1.57.0",
        "@types/node": "^24.1.0"
    }
}
